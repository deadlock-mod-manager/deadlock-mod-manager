{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://deadlockmods.app/v1/mod-config.json",
  "title": "Deadlock Mod Configuration",
  "description": "Configuration schema for Deadlock mod packages (.dmodpkg) and dmm.json metadata",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema reference"
    },
    "schema_version": {
      "type": "integer",
      "default": 1,
      "description": "Schema version for forward compatibility"
    },
    "name": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$",
      "description": "Unique identifier in kebab-case (a-z0-9-)"
    },
    "display_name": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable name"
    },
    "version": {
      "type": "string",
      "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)(?:-((?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\\.(?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\\+([0-9a-zA-Z-]+(?:\\.[0-9a-zA-Z-]+)*))?$",
      "description": "Semantic version (semver)"
    },
    "description": {
      "type": "string",
      "maxLength": 500,
      "description": "Short description (max 500 chars)"
    },
    "game_version": {
      "type": "string",
      "description": "Compatible Deadlock version constraint"
    },
    "authors": {
      "type": "array",
      "items": {
        "oneOf": [
          {
            "type": "string",
            "description": "Author name as string"
          },
          {
            "type": "object",
            "required": ["name"],
            "properties": {
              "name": {
                "type": "string",
                "description": "Author name"
              },
              "role": {
                "type": "string",
                "description": "Author role (e.g., Contributor, Artist)"
              },
              "url": {
                "type": "string",
                "format": "uri",
                "description": "Author URL"
              }
            }
          }
        ]
      },
      "description": "List of authors (string or object)"
    },
    "license": {
      "type": "string",
      "description": "SPDX license identifier"
    },
    "readme": {
      "type": "string",
      "description": "Path to README file"
    },
    "homepage": {
      "type": "string",
      "format": "uri",
      "description": "Project homepage URL"
    },
    "repository": {
      "type": "string",
      "format": "uri",
      "description": "Source code repository URL"
    },
    "screenshots": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "Screenshot path or URL"
      },
      "description": "Mod-level screenshots (paths or URLs)"
    },
    "variant_groups": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "name", "default", "variants"],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z0-9_]+$",
            "description": "Unique group identifier"
          },
          "name": {
            "type": "string",
            "description": "Display name"
          },
          "description": {
            "type": "string",
            "description": "Group description"
          },
          "default": {
            "type": "string",
            "description": "Default variant ID"
          },
          "variants": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "object",
              "required": ["id", "name", "layers"],
              "properties": {
                "id": {
                  "type": "string",
                  "pattern": "^[a-z0-9_]+$",
                  "description": "Unique variant identifier"
                },
                "name": {
                  "type": "string",
                  "description": "Display name"
                },
                "description": {
                  "type": "string",
                  "description": "Variant description"
                },
                "layers": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "description": "Which layers to enable for this variant"
                },
                "preview_image": {
                  "type": "string",
                  "description": "Main preview image (path or URL)"
                },
                "screenshots": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "description": "Additional screenshots (paths or URLs)"
                }
              }
            }
          }
        }
      },
      "description": "Variant groups define mutually exclusive options"
    },
    "layers": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "priority"],
        "properties": {
          "name": {
            "type": "string",
            "pattern": "^[a-z0-9_]+$",
            "description": "Unique layer name"
          },
          "priority": {
            "type": "number",
            "description": "Override priority (higher = wins)"
          },
          "description": {
            "type": "string",
            "description": "Layer description"
          },
          "required": {
            "type": "boolean",
            "default": false,
            "description": "Must be installed"
          }
        }
      },
      "description": "Layers are additive content modules with priority-based overrides"
    },
    "metadata": {
      "type": "object",
      "properties": {
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Mod tags for categorization"
        },
        "category": {
          "type": "string",
          "description": "Primary mod category"
        },
        "nsfw": {
          "type": "boolean",
          "default": false,
          "description": "Whether mod contains NSFW content"
        }
      },
      "description": "Additional metadata"
    },
    "dependencies": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["remote_id", "name"],
        "properties": {
          "remote_id": {
            "type": "string",
            "description": "Remote identifier of the dependency mod"
          },
          "name": {
            "type": "string",
            "description": "Human-readable name of the dependency"
          },
          "version": {
            "type": "string",
            "description": "Version constraint (e.g., >=1.0.0)"
          },
          "optional": {
            "type": "boolean",
            "default": false,
            "description": "Whether this dependency is optional"
          }
        }
      },
      "description": "Mods this mod requires"
    },
    "conflicts": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["remote_id", "name"],
        "properties": {
          "remote_id": {
            "type": "string",
            "description": "Remote identifier of the conflicting mod"
          },
          "name": {
            "type": "string",
            "description": "Human-readable name of the conflicting mod"
          }
        }
      },
      "description": "Mods this mod is incompatible with"
    },
    "breaks_on_update": {
      "type": "boolean",
      "default": false,
      "description": "Whether this mod may break when the game is updated"
    },
    "load_priority": {
      "type": "integer",
      "description": "Suggested install/load order priority (higher values load later)"
    }
  }
}
