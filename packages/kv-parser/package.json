{
  "name": "@deadlock-mods/kv-parser",
  "version": "0.2.0",
  "description": "High-performance KeyValues (VDF) parser with AST support using native Rust implementation via Bun FFI",
  "keywords": [
    "ast",
    "deadlock",
    "ffi",
    "keyvalues",
    "native",
    "parser",
    "performance",
    "rust",
    "valve",
    "vdf"
  ],
  "license": "GPL-3.0",
  "author": "stormix <contact@stormix.co>",
  "repository": {
    "type": "git",
    "url": "https://github.com/deadlock-mod-manager/deadlock-mod-manager",
    "directory": "packages/kv-parser"
  },
  "files": [
    "README.md",
    "src/",
    "target/release/kv_parser.*",
    "target/release/libkv_parser.*"
  ],
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "prebuild": "pnpm run generate-types",
    "build": "cargo build --release",
    "build:debug": "pnpm run generate-types && cargo build",
    "generate-types": "cargo test --lib --quiet || node -e \"process.exit(0)\"",
    "test": "pnpm run \"/^test:.*/\"",
    "test:ts": "bun test",
    "test:rust": "cargo test",
    "clean": "cargo clean && rm -rf src/generated/*.ts",
    "check": "cargo check",
    "prepublishOnly": "pnpm run build",
    "pretest": "pnpm run build",
    "postbuild": "pnpm run -w lint:fix"
  },
  "devDependencies": {
    "@deadlock-mods/typescript-config": "workspace:*",
    "@types/bun": "^1.3.0",
    "@types/node": "^24.5.2",
    "bun-types": "^1.3.0"
  },
  "peerDependencies": {
    "bun": ">=1.3.0"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
