{
  "name": "@deadlock-mods/hero-parser",
  "version": "1.0.0",
  "description": "Hero detection for Deadlock mod VPK files - analyzes VPK entry paths to determine which hero a mod targets",
  "license": "GPL-3.0",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "cli": "bun src/cli.ts",
    "build": "cargo build --release",
    "check": "cargo check",
    "test": "cargo test"
  },
  "dependencies": {
    "@deadlock-mods/vpk-parser": "workspace:*"
  },
  "devDependencies": {
    "@deadlock-mods/typescript-config": "workspace:*",
    "@types/bun": "^1.3.11",
    "@types/node": "^24.12.0",
    "bun-types": "^1.3.11"
  }
}
