{
  "name": "@deadlock-mods/dmp-parser",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "parse": "cargo run --bin dmp-parser-cli --release --"
  }
}
