# Enable Nix flake development environment
# Run 'direnv allow' to enable automatic environment loading
use flake

# Optional: Add node_modules/.bin to PATH for npm scripts
layout node 2>/dev/null || true