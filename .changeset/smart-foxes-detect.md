---
"@deadlock-mods/desktop": minor
---

Auto-detect NVIDIA GPU on Wayland instead of always enabling WebKit DMA-BUF workaround on Linux. The setting now supports Auto (default), Force On, and Force Off modes, preventing unnecessary performance degradation on non-NVIDIA systems.
