---
"@deadlock-mods/desktop": patch
---

Add automatic replays symlink to consolidate replay storage location. Creates a symlink from citadel/replays to citadel/addons/replays so all replays are stored in one place regardless of launch mode (modded vs vanilla).
