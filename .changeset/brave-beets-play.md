---
"@deadlock-mods/desktop": minor
---

Revamp the custom theme builder: Arcane-style ambient background layers and sidebar transparency replace button glow; compact advanced controls in accordions; draft edits preview in isolation until Apply draft; export saves thumbnail via skeleton screenshot (html-to-image) with gradient fallback; defaults aligned with app dark tokens.
