name: Release Services (API, Auth, Bot, Lockdex, Mirror)
env:
  NODE_VERSION: "24.8.0"

on:
  workflow_dispatch:
  push:
    tags:
      - "services-*"

jobs:
  create-release:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    outputs:
      release_id: ${{ steps.create-release.outputs.result }}
      api_version: ${{ steps.get-versions.outputs.api_version }}
      auth_version: ${{ steps.get-versions.outputs.auth_version }}
      bot_version: ${{ steps.get-versions.outputs.bot_version }}
      lockdex_version: ${{ steps.get-versions.outputs.lockdex_version }}
      mirror_version: ${{ steps.get-versions.outputs.mirror_version }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ssh-key: ${{ secrets.SSH_KEY }}
      - name: Setup Node.js environment
        uses: actions/setup-node@v3
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache-dependency-path: pnpm-lock.yaml
      - uses: pnpm/action-setup@v4

      - name: Get service versions
        id: get-versions
        run: |
          API_VERSION=$(jq -r '.version' ./apps/api/package.json)
          AUTH_VERSION=$(jq -r '.version' ./apps/auth/package.json)
          BOT_VERSION=$(jq -r '.version' ./apps/bot/package.json)
          LOCKDEX_VERSION=$(jq -r '.version' ./apps/lockdex/package.json)
          MIRROR_VERSION=$(jq -r '.version' ./apps/mirror-service/package.json)

          echo "API Version: $API_VERSION"
          echo "Auth Version: $AUTH_VERSION"
          echo "Bot Version: $BOT_VERSION"
          echo "Lockdex Version: $LOCKDEX_VERSION"
          echo "Mirror Version: $MIRROR_VERSION"

          echo "api_version=$API_VERSION" >> $GITHUB_OUTPUT
          echo "auth_version=$AUTH_VERSION" >> $GITHUB_OUTPUT
          echo "bot_version=$BOT_VERSION" >> $GITHUB_OUTPUT
          echo "lockdex_version=$LOCKDEX_VERSION" >> $GITHUB_OUTPUT
          echo "mirror_version=$MIRROR_VERSION" >> $GITHUB_OUTPUT

      - name: Create or get existing release
        id: create-release
        uses: actions/github-script@v6
        with:
          script: |
            const tag = context.ref.replace('refs/tags/', '');

            // First, try to get existing release by tag
            try {
              const { data: release } = await github.rest.repos.getReleaseByTag({
                owner: context.repo.owner,
                repo: context.repo.repo,
                tag: tag
              });
              
              console.log(`Found existing release: ${release.id}`);
              return release.id;
            } catch (error) {
              if (error.status === 404) {
                // Release doesn't exist, create a new one
                console.log(`Creating new release for tag: ${tag}`);
                
                const apiVersion = process.env.API_VERSION || 'unknown';
                const authVersion = process.env.AUTH_VERSION || 'unknown';
                const botVersion = process.env.BOT_VERSION || 'unknown';
                const lockdexVersion = process.env.LOCKDEX_VERSION || 'unknown';
                const mirrorVersion = process.env.MIRROR_VERSION || 'unknown';
                
                const releaseBody = `## Services Release
                
                This release includes the following services:
                
                ### 🚀 API Service
                - **Version:** v${apiVersion}
                - **Docker Image:** \`stormix/deadlock-mods-api:${apiVersion}\`
                
                ### 🔐 Auth Service
                - **Version:** v${authVersion}
                - **Docker Image:** \`stormix/deadlock-mods-auth:${authVersion}\`
                
                ### 🤖 Discord Bot
                - **Version:** v${botVersion}
                - **Docker Image:** \`stormix/deadlock-mods-bot:${botVersion}\`
                
                ### 🔍 Lockdex Service
                - **Version:** v${lockdexVersion}
                - **Docker Image:** \`stormix/deadlock-mods-lockdex:${lockdexVersion}\`
                
                ### 🪞 Mirror Service
                - **Version:** v${mirrorVersion}
                - **Docker Image:** \`stormix/deadlock-mods-mirror-service:${mirrorVersion}\`
                
                ---
                *This release was generated automatically using GitHub Actions.*`;
                
                const { data } = await github.rest.repos.createRelease({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  tag_name: tag,
                  name: `Services Release ${tag}`,
                  body: releaseBody,
                  draft: true,
                  prerelease: false
                });
                return data.id;
              } else {
                // Re-throw if it's a different error
                throw error;
              }
            }
        env:
          API_VERSION: ${{ steps.get-versions.outputs.api_version }}
          AUTH_VERSION: ${{ steps.get-versions.outputs.auth_version }}
          BOT_VERSION: ${{ steps.get-versions.outputs.bot_version }}
          LOCKDEX_VERSION: ${{ steps.get-versions.outputs.lockdex_version }}
          MIRROR_VERSION: ${{ steps.get-versions.outputs.mirror_version }}

  build-api:
    needs: create-release
    permissions:
      packages: write
      contents: read
      attestations: write
      id-token: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build and push Docker image
        uses: ./.github/actions/docker-build-push
        with:
          service-name: api
          docker-username: ${{ secrets.DOCKER_USERNAME }}
          docker-password: ${{ secrets.DOCKER_PASSWORD }}
          node-version: ${{ env.NODE_VERSION }}

      - name: Restart Kubernetes deployment
        uses: ./.github/actions/k8s-restart-deployment
        with:
          service-name: api
          kubeconfig: ${{ secrets.KUBECONFIG }}
          namespace: ${{ secrets.K8S_NAMESPACE || 'default' }}

  build-auth:
    needs: create-release
    permissions:
      packages: write
      contents: read
      attestations: write
      id-token: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build and push Docker image
        uses: ./.github/actions/docker-build-push
        with:
          service-name: auth
          docker-username: ${{ secrets.DOCKER_USERNAME }}
          docker-password: ${{ secrets.DOCKER_PASSWORD }}
          node-version: ${{ env.NODE_VERSION }}

      - name: Restart Kubernetes deployment
        uses: ./.github/actions/k8s-restart-deployment
        with:
          service-name: auth
          kubeconfig: ${{ secrets.KUBECONFIG }}
          namespace: ${{ secrets.K8S_NAMESPACE || 'default' }}

  build-bot:
    needs: create-release
    permissions:
      packages: write
      contents: read
      attestations: write
      id-token: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build and push Docker image
        uses: ./.github/actions/docker-build-push
        with:
          service-name: bot
          docker-username: ${{ secrets.DOCKER_USERNAME }}
          docker-password: ${{ secrets.DOCKER_PASSWORD }}
          node-version: ${{ env.NODE_VERSION }}

      - name: Restart Kubernetes deployment
        uses: ./.github/actions/k8s-restart-deployment
        with:
          service-name: bot
          kubeconfig: ${{ secrets.KUBECONFIG }}
          namespace: ${{ secrets.K8S_NAMESPACE || 'default' }}

  build-lockdex:
    needs: create-release
    permissions:
      packages: write
      contents: read
      attestations: write
      id-token: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build and push Docker image
        uses: ./.github/actions/docker-build-push
        with:
          service-name: lockdex
          docker-username: ${{ secrets.DOCKER_USERNAME }}
          docker-password: ${{ secrets.DOCKER_PASSWORD }}
          node-version: ${{ env.NODE_VERSION }}

      - name: Restart Kubernetes deployment
        uses: ./.github/actions/k8s-restart-deployment
        with:
          service-name: lockdex
          kubeconfig: ${{ secrets.KUBECONFIG }}
          namespace: ${{ secrets.K8S_NAMESPACE || 'default' }}

  build-mirror:
    needs: create-release
    permissions:
      packages: write
      contents: read
      attestations: write
      id-token: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build and push Docker image
        uses: ./.github/actions/docker-build-push
        with:
          service-name: mirror-service
          docker-username: ${{ secrets.DOCKER_USERNAME }}
          docker-password: ${{ secrets.DOCKER_PASSWORD }}
          node-version: ${{ env.NODE_VERSION }}

      - name: Restart Kubernetes deployment
        uses: ./.github/actions/k8s-restart-deployment
        with:
          service-name: mirror
          kubeconfig: ${{ secrets.KUBECONFIG }}
          namespace: ${{ secrets.K8S_NAMESPACE || 'default' }}

  publish-release:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    needs:
      [
        create-release,
        build-api,
        build-auth,
        build-bot,
        build-lockdex,
        build-mirror,
      ]

    steps:
      - name: Publish release
        uses: actions/github-script@v6
        env:
          release_id: ${{ needs.create-release.outputs.release_id }}
        with:
          script: |
            try {
              console.log(`Updating release ID: ${process.env.release_id}`);
              
              // First get the current release to check its state
              const { data: currentRelease } = await github.rest.repos.getRelease({
                owner: context.repo.owner,
                repo: context.repo.repo,
                release_id: process.env.release_id
              });
              
              console.log(`Current release state - Draft: ${currentRelease.draft}, Tag: ${currentRelease.tag_name}`);
              
              // Update the release to publish it
              const { data: updatedRelease } = await github.rest.repos.updateRelease({
                owner: context.repo.owner,
                repo: context.repo.repo,
                release_id: process.env.release_id,
                draft: false,
                prerelease: false
              });
              
              console.log(`Successfully published release: ${updatedRelease.html_url}`);
              return updatedRelease.id;
            } catch (error) {
              console.error(`Failed to publish release: ${error.message}`);
              console.error(`Error details:`, error);
              throw error;
            }

  cleanup-release:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    needs:
      [
        create-release,
        build-api,
        build-auth,
        build-bot,
        build-lockdex,
        build-mirror,
      ]
    if: failure()

    steps:
      - name: Delete release
        uses: actions/github-script@v6
        env:
          release_id: ${{ needs.create-release.outputs.release_id }}
        with:
          script: |
            github.rest.repos.deleteRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              release_id: process.env.release_id
            })
